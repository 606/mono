# Se.FeatureFlags.Api

Minimal API for feature flags management (MVP).

- .NET 8 Minimal API
- SQLite or JSON file storage
- In-memory cache
- API: /flags, /flags/{key}, /healthz, etc.

See requirements in project documentation.
